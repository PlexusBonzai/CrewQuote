# Copy to .env.local on the trusted build machine and keep values out of git.
# These are browser-safe Supabase values. Never place a secret/service-role key,
# database password, or SMTP credential in a VITE_ variable.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
