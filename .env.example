# Copy to .env.local for local Supabase browser-client testing.
# These values are safe to expose in browser code, but real project-specific
# values should stay out of git.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
